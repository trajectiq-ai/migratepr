{
  "name": "checkout-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Demo repo for MigratePR: Stripe v12 usage whose suite must stay green across the v13 migration",
  "scripts": {
    "test": "node node_modules/jest/bin/jest.js"
  },
  "dependencies": {
    "stripe": "^12.18.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
